while( *str != '\0') {

  if (isdigit(*str)) {
     int t = getInt(str);
     push(t)
     str = nextSpace(str)
  }       else {
    switch (*str) {
    case '+':
        
    }
  }

  str++;
}


10 10 + 20 *

10
10
20

+ 
pop 20 
pop 10